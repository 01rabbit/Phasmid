# Archive Review Test Profile

These tests are historical/evaluation-oriented and are not part of the default CI path.
Keep them for traceability and periodic review.

Run manually when needed:

```bash
python3 -m unittest discover -s tests_archive_review
```
