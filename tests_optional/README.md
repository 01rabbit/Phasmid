# Optional Test Profile

These tests are valuable but require optional dependencies, hardware-adjacent setup,
or long-running execution compared to the default test profile.

Run manually when needed:

```bash
python3 -m unittest discover -s tests_optional
```
